# Skip node-gyp compilation for packages that ship prebuilt binaries.
# @parcel/watcher ships platform-specific prebuilt packages; this prevents
# npm from falling back to compiling from source (which requires Python distutils).
optional=true
ignore-scripts=false
node-gyp=node-gyp
shamefully-hoist=true