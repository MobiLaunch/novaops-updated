# Agent guidance (NovaOps)

## Layout & UI audit

For UI bugs, Vuetify migration gaps, layout consistency, and polish:

1. Read and follow `.cursor/skills/novaops-layout-ui/SKILL.md`
2. Run `node scripts/ui-audit.mjs` before and after fixes

Trigger phrases: "UI audit", "layout review", "find UI bugs", "polish the UI".
